# (DEPRECATED) Checklist Flow PLANIZZA

Ce fichier est **historique** et n’est plus maintenu.

Pourquoi :

- Routes et UX ont évolué (`/panier` au lieu de `/cart`, détail camion via `/:truckId` slug/RTDB key).
- L’avancement automatique des statuts via scheduler est **désactivé** (workflow manuel côté pizzaiolo).
- Le “checkout invité” n’est pas la référence produit actuelle.

✅ Référence unique pour valider l’app après refactor : `CHECKLIST_SMOKE_TEST.md`.
